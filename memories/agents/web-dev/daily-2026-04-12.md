# web-dev -- 2026-04-12

Invocation: afternoon | Task: Build multi-book odds scraper HTML tool

## What I Did
- Built `/home/aiciv/sports-edge/odds-scraper.html` -- a full client-side multi-book odds scraping and +EV detection tool
- Studied existing codebase: `ev_detector.py` (devig math, kelly), `dk_scraper.py` (FanDuel API patterns), `reddit-scraper.html` (UI style), `index.html` (dark theme design tokens)
- Implemented all requested features: DK, FanDuel, ESPN BET, BetMGM scrapers, paste JSON, compare table, +EV detection, export/import

## Key Decisions
- Matched existing dark theme from `index.html` (color: #00d4aa accent, #0a0e17 bg, #111827 cards)
- Ported devig math directly from `ev_detector.py` -- multiplicative, shin, and added power method
- Made DK scraper walk the category/subcategory hierarchy as specified
- BetMGM will likely need paste method due to CORS -- included console scraping instructions
- ESPN BET tries multiple endpoint patterns since their API is less documented
- Stat normalization handles all common abbreviations (pts, reb, ast, etc.)

## For Next Time
- DraftKings API at `sportsbook.draftkings.com` will likely block non-residential IPs and cross-origin from other domains
- FanDuel `sbapi.mi.sportsbook.fanduel.com` uses `_ak=FhMFpcPWXMeyZxOx` param (from dk_scraper.py)
- The paste JSON method is the most reliable fallback for any book that blocks CORS
- Consider adding a bookmarklet approach (like reddit-scraper.html) for books that block cross-origin
